#include "toptier.h"
#include "main.h"
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	char *sh = "Toptier";

	while(*sh)
	{
		_putchar(*sh);
		sh++;
	}
	_putcher('\n');
	return (0);
}
